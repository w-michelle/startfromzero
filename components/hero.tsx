const Hero = () => {
  return (
    <div
      style={{ backgroundImage: `url(${"/hero.png"})` }}
      className="relative top-0 left-0 h-screen bg-cover z-[-10]"
    ></div>
  );
};

export default Hero;
